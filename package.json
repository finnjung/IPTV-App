{
  "name": "streameee-deploy",
  "version": "1.0.0",
  "description": "Deploy scripts for streameee Fire TV App",
  "private": true,
  "scripts": {
    "deploy": "node deploy-apk.js",
    "deploy:build": "node deploy-apk.js --build",
    "release": "node deploy-apk.js --build"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "dotenv": "^16.4.7"
  }
}
